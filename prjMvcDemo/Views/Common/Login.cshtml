
@{
    ViewBag.Title = "Login";
}

<h2>登入系統</h2>                


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

<div class="form-horizontal">
    <h4>輸入帳號與密碼</h4>
    <hr />
    <input type="hidden" name="txtFId" value="@ViewBag.FId" />
    <div class="form-group">
        <div class="col-md-offset-0 col-md-10">
            帳號(註冊時填的Email)
        </div>
        <div class="col-md-10">
            @Html.TextBox("txtAccount", "marco@gmail.com", new { htmlAttributes = new { @class = "form-control" } })
        </div>
    </div>
    <input type="hidden" name="txtFId" value="@ViewBag.FId" />
    <div class="form-group">
        <div class="col-md-offset-0 col-md-10">
            密碼
        </div>
        <div class="col-md-10">
            @Html.TextBox("txtPassword", "", new { htmlAttributes = new { @class = "form-control" } })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-0 col-md-10">
            <input type="submit" value="登入" class="btn btn-default" />
        </div>
    </div>
</div>
}

<div>
    @Html.ActionLink("註冊新帳號", "create")
</div>

<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
